apiVersion: networking.k8s.io/v1
kind: ingress

metadata:
  name: gitops-ingress

spec:
  rules:
  - host: gitops.local
    http: 
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gitops-service
            port: 
              number: 3000
              